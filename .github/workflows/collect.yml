name: RSS 수집

on:
  # ⛔ 2026-09-07 자동 수집 중단
  #
  # 블로그를 peanutradio.github.io 로 통합하면서 RSS 자동수집을 껐다.
  # 수집된 글 2,209편과 이 저장소는 보존하되, 새로 쌓지 않는다.
  # 되살리려면 아래 schedule 블록의 주석을 풀면 된다.
  # (수동 실행은 workflow_dispatch 로 계속 가능)
  #
#  schedule:
#    # Azure       : 월/목 09:00 KST (= 00:00 UTC) — --board ms
#    - cron: '0 0 * * 1,4'
#    # GitHub      : 월/목 12:00 KST (= 03:00 UTC) — --board ms
#    - cron: '0 3 * * 1,4'
#    # AI Foundry  : 화/금 09:00 KST (= 00:00 UTC) — --board ms
#    - cron: '0 0 * * 2,5'
#    # Fabric      : 화/금 12:00 KST (= 03:00 UTC) — --board ms
#    - cron: '0 3 * * 2,5'
#    # 보안 & 컴플라이언스: 수/토 09:00 KST (= 00:00 UTC) — --board ms
#    - cron: '0 0 * * 3,6'
#    # M365 & Copilot     : 수/토 12:00 KST (= 03:00 UTC) — --board ms
#    - cron: '0 3 * * 3,6'
#    # IT 소식     : 매일 15:00 KST (= 06:00 UTC) — --board it
#    - cron: '0 6 * * *'
#    # 주식/크립토 시황 : 매일 09:00 KST (= 00:00 UTC) — --board market
#    - cron: '30 0 * * *'

  workflow_dispatch:
    inputs:
      board:
        description: '수집할 게시판 (all / it / ms / market)'
        required: false
        default: 'all'

permissions:
  contents: write

concurrency:
  group: collect
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Python 환경 세팅
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: scripts/requirements.txt

      - name: 의존성 설치
        run: pip install -r scripts/requirements.txt

      - name: 수집 게시판 결정
        id: board
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "board=${{ github.event.inputs.board }}" >> "$GITHUB_OUTPUT"
          else
            HOUR=$(date -u +%H)
            MINUTE=$(date -u +%M)
            BOARD="all"
            # 매일 06:00 UTC → IT 소식 (15:00 KST)
            if [[ "$HOUR" == "06" ]]; then
              BOARD="it"
            # 00:30 UTC → 주식/크립토 시황 (09:30 KST)
            elif [[ "$HOUR" == "00" && "$MINUTE" -ge 25 ]]; then
              BOARD="market"
            # 00:00 / 03:00 UTC → MS 게시판
            elif [[ "$HOUR" == "00" || "$HOUR" == "03" ]]; then
              BOARD="ms"
            fi
            echo "board=$BOARD" >> "$GITHUB_OUTPUT"
          fi

      - name: RSS 수집
        if: steps.board.outputs.board != 'market'
        run: python scripts/collect_rss.py --board ${{ steps.board.outputs.board }}

      - name: 시황 수집
        if: steps.board.outputs.board == 'market' || steps.board.outputs.board == 'all'
        env:
          COINGECKO_API_KEY: ${{ secrets.COINGECKO_API_KEY }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: python scripts/collect_market.py

      - name: 새 포스트 커밋 & 푸시
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add content/ scripts/.published_urls.json 2>/dev/null || git add content/
          if git diff --cached --quiet; then
            echo "새 포스트 없음 — 커밋 생략"
          else
            POST_COUNT=$(git diff --cached --name-only -- 'content/**' | wc -l | tr -d ' ')
            git commit -m "feat: 새 포스트 ${POST_COUNT}건 수집 [board: ${{ steps.board.outputs.board }}]

          Co-Authored-By: Paperclip <noreply@paperclip.ing>"
            git pull --rebase
            git push
          fi
